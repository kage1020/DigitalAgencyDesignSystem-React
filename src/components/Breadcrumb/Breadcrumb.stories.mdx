import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import Breadcrumb from './Breadcrumb'

<Meta
  title='components/Breadcrumb'
  component={Breadcrumb}
  args={{
    links: [
      {
        label: 'Home',
        path: '#',
      },
      {
        label: '政策',
        path: '#',
      },
      {
        label: '審議会・研究会',
        path: '#',
      },
      {
        label: 'デジタル庁における入札制限等の在り方に関する検討会',
        path: '#',
      },
    ],
  }}
/>

export const Template = (args) => <Breadcrumb {...args} />

# Breadcrumb

## 標準コンポーネント

下階層へ遷移した際に、ユーザー自身が閲覧しているページが全体の階層構造内のどこに位置しているかを示す事を目的とします。

<Canvas isColumn>
  <Story name='Normal'>{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Breadcrumb} />
