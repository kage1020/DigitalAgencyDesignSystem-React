import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

import Input from './Input'

<Meta
  title='components/Input'
  component={Input}
  args={{ name: 'test', label: 'ラベル', placeholder: 'プレイスホルダー' }}
/>

export const Template = (args) => <Input {...args} />

# Input

テキスト入力コンポーネントは、名前や電話番号など、1 行以内のテキストを入力する場合に使用します。

<Canvas isColumn>
  <Story name='Default'>{Template.bind({})}</Story>
  <Story
    name='Filled'
    args={{
      value: '入力済みの内容',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Error'
    args={{
      required: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Disabled'
    args={{
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Input} />
